/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: [
      "shikimori.one",
      "cdn.shadcnstudio.com",
      "lh3.googleusercontent.com",
    ],
  },
};

export default nextConfig;
